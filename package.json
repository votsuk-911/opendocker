{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opendocker",
  "description": "A CLI tool for managing Docker",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.6",
  "scripts": {
    "dev": "bun run --conditions=browser ./packages/cli/src/index.tsx",
    "test": "echo 'Hello World!'"
  },
  "workspaces": [
    "packages/*"
  ]
}
